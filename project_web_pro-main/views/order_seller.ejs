<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>จัดการคำสั่งซื้อ</title>
    <link rel="stylesheet" href="/css/order_seller.css">
    <style>
        /* CSS เพิ่มเติมสำหรับปุ่มลบ */
        .btn-delete {
            background-color: #e74c3c; /* สีแดง */
            color: white;
            border: none;
            padding: 8px 12px;
            border-radius: 5px;
            cursor: pointer;
        }
        .btn-delete:hover {
            background-color: #c0392b;
        }
    </style>
</head>

<body>
    <nav id="navbar">
        <div class="navbar-header">
            <img src="/images/logo.png" alt="MONGCHA Logo" class="navbar-brand">
            <button id="menu-toggle" class="menu-toggle">☰</button>
        </div>
        <ul class="navbar-menu">
            <li><a href="/order_seller">จัดการคำสั่งซื้อ</a></li>
            <li><a href="/addmenu">จัดการเมนู</a></li>
            <li><a href="/logout">ลงชื่อออก</a></li>
        </ul>
    </nav>

    <div class="container">
        <h1>คำสั่งซื้อ</h1>
        <% if (orders && orders.length > 0) { %>
            <table>
                <thead>
                    <tr>
                        <th>หมายเลขคำสั่งซื้อ</th>
                        <th>วันเวลา</th>
                        <th>ราคา</th>
                        <th>รายละเอียด</th>
                        <th>สถานะปัจจุบัน</th>
                        <th>อัปเดตสถานะ</th>
                        <th>ลบคำสั่งซื้อ</th> </tr>
                </thead>
                <tbody>
                    <% orders.forEach(order => { %>
                        <tr>
                            <td>หมายเลขสั่งซื้อ <%= order.order_id %></td>
                            <td><%= new Date(order.datetime).toLocaleString('th-TH') %></td>
                            <td><%= order.total_price.toFixed(2) %></td>
                            <td><button class="description" onclick="viewDetails('<%= order.order_id %>')">ดูรายละเอียด</button></td>
                            <td><strong><%= order.status %></strong></td>
                            <td>
                                <form class="status-form" action="/order/<%= order.order_id %>/update-status" method="POST">
                                    <select name="status">
                                        <option value="pending" <%= order.status === 'pending' ? 'selected' : '' %>>รอดำเนินการ</option>
                                        <option value="processing" <%= order.status === 'processing' ? 'selected' : '' %>>กำลังเตรียม</option>
                                        <option value="completed" <%= order.status === 'completed' ? 'selected' : '' %>>สำเร็จ</option>
                                    </select>
                                    <button type="submit">อัปเดต</button>
                                </form>
                            </td>
                            <td>
                                <form action="/order/<%= order.order_id %>/delete" method="POST" onsubmit="return confirm('คุณแน่ใจหรือไม่ว่าต้องการลบออเดอร์นี้? ข้อมูลทั้งหมดที่เกี่ยวข้องจะถูกลบอย่างถาวร');">
                                    <button type="submit" class="btn-delete">ลบ</button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else { %>
            <p>ไม่พบคำสั่งซื้อ</p>
        <% } %>
    </div>

    <script>
        document.getElementById('menu-toggle').addEventListener('click', function () {
            document.querySelector('.navbar-menu').classList.toggle('active');
        });

        function viewDetails(orderId) {
            window.location.href = `/show_order_seller/${orderId}`;
        }
    </script>
</body>
</html>