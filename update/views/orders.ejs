<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MongCha</title>
    <link rel="icon" type="image/png" href="/images/logo.png">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;500;700&display=swap" rel="stylesheet">
</head>

<body class="bg-[#74512D] min-h-screen flex flex-col font-[Kanit]">
    <!-- Navbar -->
    <%- include("partials/navbar_user") %>

        <!-- ส่วนเนื้อหาหลัก -->
        <main class="flex justify-center items-center flex-1 px-6 mb-10 mt-5">
            <div class="bg-[#F8F4E1] p-8 rounded-2xl shadow-xl w-full max-w-4xl text-center">
                <h1 class="text-3xl font-extrabold text-[#543310] mb-4">คำสั่งซื้อของคุณ</h1>

                <!-- ตารางคำสั่งซื้อ -->
                <div class="overflow-x-auto">
                    <table class="w-full border border-gray-300 rounded-lg">
                        <thead>
                            <tr class="bg-[#e6d7b9] text-[#543310]">
                                <th class="py-2 px-4 border">หมายเลขคำสั่งซื้อ</th>
                                <th class="py-2 px-4 border">วันเวลา</th>
                                <th class="py-2 px-4 border">ราคา</th>
                                <th class="py-2 px-4 border">รายละเอียด</th>
                            </tr>
                        </thead>
                        <tbody class="text-gray-700">
                            <% if (orders && orders.length> 0) { %>
                                <% orders.forEach(order=> { %>
                                    <tr class="hover:bg-[#f3ead6] transition">
                                        <td class="py-2 border">หมายเลขสั่งซื้อ <%= order.order_id %>
                                        </td>
                                        <td class="py-2 border">
                                            <%= order.datetime %>
                                        </td>
                                        <td class="py-2 border text-[#543310] font-bold">฿<%= order.total_price %>
                                        </td>
                                        <td class="py-2 border">
                                            <button onclick="viewDetails('<%= order.order_id %>')"
                                                class="bg-[#8B5E3B] text-white px-4 py-1 rounded-full hover:bg-[#6A4525] transition">
                                                ดูรายละเอียด
                                            </button>
                                        </td>
                                    </tr>
                                    <% }) %>
                                        <% } else { %>
                                            <tr>
                                                <td colspan="4" class="py-4 text-gray-500">ยังไม่มีคำสั่งซื้อ</td>
                                            </tr>
                                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>

        <!-- Script -->
        <script>
            // ฟังก์ชันดูรายละเอียดคำสั่งซื้อ
            function viewDetails(orderId) {
                window.location.href = `/show-order-user/${orderId}`;
            }
        </script>
</body>

</html>
