<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MongCha</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;500;700&display=swap" rel="stylesheet">
</head>

<body class="bg-[#74512D] min-h-screen flex items-center justify-center font-[Kanit]">
  <main class="p-8 w-full max-w-md text-center">
    <!-- โลโก้ -->
    <img src="/images/logo.png" alt="MongCha Logo" class="mx-auto w-48 mb-6">

    <!-- ฟอร์มเข้าสู่ระบบ -->
    <form id="login-form" action="/login" method="POST" class="flex flex-col gap-4">
      <input 
        type="text" 
        id="username" 
        name="username" 
        placeholder="ชื่อผู้ใช้" 
        required
        class="w-full p-3 rounded-lg border border-[#AF8F6F] bg-[#F8F4E1] placeholder-[#AF8F6F] text-[#543310] focus:outline-none focus:ring-2 focus:ring-[#AF8F6F]  shadow-md"
      >
      
      <input 
        type="password" 
        id="password" 
        name="password" 
        placeholder="รหัสผ่าน" 
        required
        class="w-full p-3 rounded-lg border border-[#AF8F6F] bg-[#F8F4E1] placeholder-[#AF8F6F] text-[#543310] focus:outline-none focus:ring-2 focus:ring-[#AF8F6F] shadow-md"
      >

      <button 
        type="submit"
        class="w-full bg-[#543310] text-[#F8F4E1] py-3 rounded-full hover:bg-[#6A4525] transition"
      >
        เข้าสู่ระบบ
      </button>
    </form>

    <!-- ลิงก์สมัครสมาชิก -->
    <div class="mt-6 text-[#AF8F6F] flex justify-center gap-2">
      <p>ยังไม่มีบัญชีใช่ไหม?</p>
      <button 
        onclick="goregispage('/register')" 
        class="text-[#F8F4E1] hover:underline"
      >
        ลงชื่อสมัคร
      </button>
    </div>
  </main>

  <script>
    // ฟังก์ชันไปหน้าสมัครสมาชิก
    function goregispage(url) {
      window.location.href = url;
    }
  </script>
</body>
</html>
